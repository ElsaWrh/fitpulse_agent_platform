# Actual production env file (secrets should be secured)
MYSQL_ROOT_PASSWORD=change_me_strong_password
MYSQL_DATABASE=fitpulse_db
MYSQL_PORT=3306
TZ=Asia/Shanghai

BACKEND_PORT=8080
FRONTEND_PORT=80

# Public backend URL for browser calls
VITE_API_BASE=https://api.fitpulse.com/api

# Restart policy for containers (no|always|on-failure|unless-stopped)
RESTART_POLICY=always
