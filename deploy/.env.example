# ==========================================
# FitPulse Agent Platform - 环境配置模板
# ==========================================
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 根据实际情况修改配置值
# 3. .env 文件包含敏感信息，已在 .gitignore 中排除
# ==========================================

# ============ 项目配置 ============
PROJECT_NAME=fitpulse
NODE_ENV=production
SPRING_PROFILES_ACTIVE=prod

# ============ 数据库配置 ============
MYSQL_ROOT_PASSWORD=123456
MYSQL_DATABASE=fitpulse_db
MYSQL_PORT=3306

# ============ 服务端口 ============
BACKEND_PORT=8080
WEB_ADMIN_PORT=80
PHPMYADMIN_PORT=8081

# ============ 前端 API 配置 ============
# 开发环境：http://localhost:8080/api
# 生产环境：https://your-domain.com/api
VITE_API_BASE=http://localhost:8080/api

# ============ LLM API 配置 ============
# OpenAI API Key（如果使用 OpenAI）
OPENAI_API_KEY=

# 阿里云通义千问 DashScope API Key（推荐，中国大陆可用）
DASHSCOPE_API_KEY=

# ============ Java 运行参数 ============
JAVA_OPTS=-Xms512m -Xmx1g

# ============ Docker 配置 ============
# 时区配置
TZ=Asia/Shanghai

# 容器重启策略：no | always | on-failure | unless-stopped
RESTART_POLICY=unless-stopped
